/**
 * Refactoring function. Returns Math.hypot(values).
 * @param {number[]} values [values]
 * @returns Number
 */
export default function magnitude(...values: number[]): number { return Math.hypot(...values) }